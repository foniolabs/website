import type { SchemaTypeDefinition } from "sanity";

// Day 2 will populate this with document types (page, post, author, product,
// teamMember, siteSettings, navigation, seo, redirect) and reusable section
// blocks (hero, featureGrid, richText, cta, testimonial, logoCloud, embedHtml,
// contactForm).
export const schemaTypes: SchemaTypeDefinition[] = [];
